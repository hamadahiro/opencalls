# robots.txt is ADVISORY. Well-behaved crawlers honour it; the headless fleet
# that hit this site on 29-30 Jul 2026 never requested this file. Blocking a
# scraper requires something in front of the origin — see docs/anti-scraping.md.

User-agent: *
Allow: /
Disallow: /generate-pages.js

# --- Bulk AI training crawlers ---
# These harvest for model training and send no traffic back. Nothing to gain by
# allowing them. Google-Extended and Applebot-Extended govern AI training ONLY —
# neither affects Googlebot or Applebot search indexing, so ranking is untouched.
User-agent: GPTBot
User-agent: ClaudeBot
User-agent: anthropic-ai
User-agent: Claude-Web
User-agent: CCBot
User-agent: Bytespider
User-agent: Amazonbot
User-agent: Meta-ExternalAgent
User-agent: FacebookBot
User-agent: cohere-ai
User-agent: Diffbot
User-agent: Omgilibot
User-agent: ImagesiftBot
User-agent: Timpibot
User-agent: Google-Extended
User-agent: Applebot-Extended
Disallow: /

# --- Deliberately NOT blocked ---
# OAI-SearchBot, ChatGPT-User and PerplexityBot answer user queries and cite the
# source, so they send real referral traffic. Discovery is this site's whole
# acquisition channel — blocking them would cost visitors to stop a crawl that
# costs nothing. Revisit only if they start hammering.
#
# /data.json is also NOT listed. It is already preloaded from every page, so
# listing it hides nothing and merely advertises the endpoint to anyone reading
# this file. The Disallow: / above already covers it for every compliant crawler.

Sitemap: https://opencalls.monographica.com/sitemap.xml
